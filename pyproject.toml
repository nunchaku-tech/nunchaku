[build-system]
requires = [
    "setuptools",
    "wheel",
    "ninja",
]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["version"]
name = "nunchaku"
dependencies = [
    "diffusers>=0.30.3",
    "transformers",
    "accelerate",
    "sentencepiece",
    "protobuf",
]